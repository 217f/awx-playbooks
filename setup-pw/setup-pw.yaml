---
- name: windows user add
  hosts: all
  vars:
    usr_name: 'Testuser'
    usr_password: 'EfY#Qs9*BWy9?'
    usr_groups: "Remotedesktopbenutzer"
  tasks:
    - name: create local user
      ansible.windows.win_user:
        name: "{{ usr_name }}"
        password: "{{ usr_password }}"
        groups: "{{ usr_groups }}"
        update_password: on_create
        password_expired: true